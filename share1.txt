Create an ansible task that verifies on a windows server that the GPO "Interactive Logo: Machine inactivity limit" is set to 900 seconds.
If it is set to 900 seconds, the ansible task passes.  

Side question: is the task create differently if the GPO is set at the domain level or the local level or is it the same either way?
